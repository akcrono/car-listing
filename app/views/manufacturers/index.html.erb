 <ul>
  <% @manufacturers.each do |manufacturer| %>
    <li><%= link_to manufacturer.name, manufacturer_path(manufacturer)%></li>
  <% end %>
</ul>
<%= link_to "Add manufacturer", new_manufacturer_path, class: "button" %>
